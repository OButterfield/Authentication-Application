spring:
  application:
    name: Authentication
  data:
    mongodb:
      uri: mongodb://localhost:27017
      database: authentication_db

springdoc:
  swagger-ui:
    enabled: true
    url: /openapi.yml

server:
  port: 8080
  servlet:
    context-path: /api/v1
